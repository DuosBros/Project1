const initialState = {
}

const OrdersHandleReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default OrdersHandleReducer;